import { Component } from '@angular/core';
import { bootstrap }    from '@angular/platform-browser-dynamic';

@Component({
  selector: 'explorer',
  templateUrl: "src/app/sidebar/sidebar.html"
})
export class SidebarComponent { }

// bootstrap(SidebarComponent);