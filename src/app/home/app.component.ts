import { Component } from '@angular/core';
import { bootstrap }    from '@angular/platform-browser-dynamic';

@Component({
  selector: 'my-app',
  template: '<h1>My First Angular home</h1>'
})
export class HomeComponent { }

// bootstrap(HomeComponent);